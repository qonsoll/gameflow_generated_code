import { Dimensions } from 'react-native'

const DIMENSIONS = Dimensions.get('window')
const WINDOW_WIDTH = DIMENSIONS.width
const WINDOW_HEIGHT = DIMENSIONS.height

export { DIMENSIONS, WINDOW_WIDTH, WINDOW_HEIGHT }
