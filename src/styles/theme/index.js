import { NATIVE_BASE_THEME, THEME } from './mediators'

export const theme = THEME
export const nativeBaseTheme = NATIVE_BASE_THEME

export default {
  theme: THEME,
  nativeBaseTheme: NATIVE_BASE_THEME
}
